stages:
  - test
  - deploy

lint:
  image: quay.imanuel.dev/dockerhub/library---node:latest
  stage: test
  script:
    - npm install
    - npm run lint

pages:
  image: quay.imanuel.dev/dockerhub/library---node:latest
  stage: deploy
  script:
    - mkdir public
    - cp *.css public/
    - cp *.js public/
    - cp *.html public/
    - cp *.png public/
    - cp *.ico public/
    - cp fonts public/
    - echo "The site will be deployed to $CI_PAGES_URL"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_TAG

publish:
  image: quay.imanuel.dev/dockerhub/library---node:latest
  stage: deploy
  script:
    - npm install
    - npm publish --verbose --access public
  rules:
    - if: $CI_COMMIT_TAG
