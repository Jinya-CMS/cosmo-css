{{#> layout title="Theme support" mainActive="cosmo" subActive="theme" }}
    <h1 class="cosmo-title">Supported themes</h1>
    <p>
        For some time now, browsers have supported an automatic switch between dark and light theme. This switch is
        based on the theme setting in your operating system. There might be situations where you want to force a light
        or dark theme. For that purpose, Cosmo supports a forced theme. Check below to see which classes you need to
        apply.
    </p>
    <h2>Force a theme</h2>
    <p>
        To force the light theme, apply the <code>is--light</code> to the HTML tag of your document. On the other hand,
        you need to apply the <code>is--dark</code> class to the HTML tag to force the dark theme.
    </p>
    <p>
        <button id="toggleTheme" class="cosmo-button">Toggle theme</button>
    </p>
    <h2>Apply theme only to dialogs</h2>
    <p>
        It is also possible to force a theme on modal dialogs, by applying the theme to the dialog container. See below
        for an example.
    </p>
    <div class="cosmo-toolbar">
        <div class="cosmo-toolbar__group">
            <button id="lightThemeDialog" class="cosmo-button">Open light theme dialog</button>
            <button id="darkThemeDialog" class="cosmo-button">Open dark theme dialog</button>
        </div>
    </div>
    <div class="cosmo-modal__container is--dark" style="display: none">
        <div class="cosmo-modal">
            <h1 class="cosmo-modal__title">Dark themed dialog</h1>
            <div class="cosmo-modal__content">
                <div class="cosmo-input__group">
                    <label for="textInput" class="cosmo-label">Text input</label>
                    <input type="text" id="textInput" class="cosmo-input"/>
                    <div class="cosmo-checkbox__group">
                        <input type="checkbox" id="checkboxInput" class="cosmo-checkbox"/>
                        <label for="checkboxInput">Checkbox input</label>
                    </div>
                    <label class="cosmo-label cosmo-label--radio">Radios</label>
                    <div class="cosmo-radio__group">
                        <input name="radio" type="radio" id="radioInput1" class="cosmo-radio"/>
                        <label for="radioInput1">Radio input 1</label>
                        <input name="radio" type="radio" id="radioInput2" class="cosmo-radio"/>
                        <label for="radioInput2">Radio input 2</label>
                        <input name="radio" type="radio" id="radioInput3" class="cosmo-radio"/>
                        <label for="radioInput3">Radio input 3</label>
                    </div>
                </div>
            </div>
            <div class="cosmo-modal__button-bar">
                <button data-close="dark" id="cancel" class="cosmo-button">Cancel</button>
                <button data-close="dark" id="save" class="cosmo-button">Save changes</button>
            </div>
        </div>
    </div>
    <div class="cosmo-modal__container is--light" style="display: none">
        <div class="cosmo-modal">
            <h1 class="cosmo-modal__title">Light themed dialog</h1>
            <div class="cosmo-modal__content">
                <div class="cosmo-input__group">
                    <label for="textInput1" class="cosmo-label">Text input</label>
                    <input type="text" id="textInput1" class="cosmo-input"/>
                    <div class="cosmo-checkbox__group">
                        <input type="checkbox" id="checkboxInput1" class="cosmo-checkbox"/>
                        <label for="checkboxInput1">Checkbox input</label>
                    </div>
                    <label class="cosmo-label cosmo-label--radio">Radios</label>
                    <div class="cosmo-radio__group">
                        <input name="radio" type="radio" id="radioInput11" class="cosmo-radio"/>
                        <label for="radioInput11">Radio input 1</label>
                        <input name="radio" type="radio" id="radioInput21" class="cosmo-radio"/>
                        <label for="radioInput21">Radio input 2</label>
                        <input name="radio" type="radio" id="radioInput31" class="cosmo-radio"/>
                        <label for="radioInput31">Radio input 3</label>
                    </div>
                </div>
            </div>
            <div class="cosmo-modal__button-bar">
                <button data-close="light" class="cosmo-button">Cancel</button>
                <button data-close="light" class="cosmo-button">Save changes</button>
            </div>
        </div>
    </div>
    <script>
        document.getElementById('toggleTheme').addEventListener('click', () => {
            const html = document.querySelector('html');
            if (html.classList.contains('is--dark')) {
                html.classList.add('is--light');
                html.classList.remove('is--dark');
            } else if (html.classList.contains('is--light')) {
                html.classList.add('is--dark');
                html.classList.remove('is--light');
            } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
                html.classList.add('is--light');
                html.classList.remove('is--dark');
            } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches) {
                html.classList.add('is--dark');
                html.classList.remove('is--light');
            }
        });
        document.getElementById('lightThemeDialog').addEventListener('click', () => {
            document.querySelector('.cosmo-modal__container.is--light').removeAttribute('style');
        });
        document.getElementById('darkThemeDialog').addEventListener('click', () => {
            document.querySelector('.cosmo-modal__container.is--dark').removeAttribute('style');
        });
        document.querySelectorAll('[data-close=light]').forEach((item) => item.addEventListener('click', () => {
            document.querySelector('.cosmo-modal__container.is--light').style.display = 'none';
        }));
        document.querySelectorAll('[data-close=dark]').forEach((item) => item.addEventListener('click', () => {
            document.querySelector('.cosmo-modal__container.is--dark').style.display = 'none';
        }));
    </script>
{{/layout}}